---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-docker-cluster
      image: ubuntu
      image_version: 16:04
      ansible_groups:
        - group1
    - name: ansible-docker-cluster
      image: centos
      image_version: 7
      ansible_groups:
        - group1        
verifier:
  name: testinfra
